import Layout from "../layout/Layout";

export default function Result({
  result = "PASS",
  skill = "Electrician",
  feedback = "Great! You followed all safety precautions.",
  improvement = "Review electrical safety guidelines and proper wire handling techniques.",
}) {
  const isPass = result === "PASS";

  return (
    <Layout>
      <div className="flex justify-center items-center min-h-[70vh]">
        <div className="bg-white dark:bg-slate-800 rounded-2xl shadow-2xl p-10 max-w-lg w-full">
          {/* Result Header */}
          <h1
            className={`text-5xl font-bold mb-6 text-center ${
              isPass ? "text-green-500" : "text-red-500"
            }`}
          >
            {isPass ? "PASS ✅" : "FAIL ❌"}
          </h1>

          {/* Skill Section */}
          <p className="text-gray-600 dark:text-gray-300 text-center text-lg mb-6">
            Skill: <span className="font-semibold">{skill}</span>
          </p>

          {/* AI Feedback Card */}
          <div className="bg-gray-100 dark:bg-slate-700 p-6 rounded-xl shadow-inner">
            <h2 className="text-xl font-semibold mb-4 text-gray-800 dark:text-white">
              AI Feedback
            </h2>

            <p className="text-gray-700 dark:text-gray-200 mb-3">{feedback}</p>

            {!isPass && (
              <p className="text-yellow-500">
                Suggested Improvement: {improvement}
              </p>
            )}
          </div>

          {/* AI Note */}
          <div className="mt-6 bg-gray-50 dark:bg-slate-900 border border-gray-300 dark:border-gray-600 rounded-xl p-4 text-gray-500 dark:text-gray-400 italic">
            💡 This feedback is generated by SkillSetu AI to help you improve
            and verify your skills.
          </div>
        </div>
      </div>
    </Layout>
  );
}
